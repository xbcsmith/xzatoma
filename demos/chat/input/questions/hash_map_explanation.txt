Write a brief description of what a hash map is and how it works.

Answer: A hash map is a data structure that maps keys to values using a hash function to compute an index into an array. It stores key-value pairs in buckets; when multiple keys hash to the same index, collisions are handled via techniques like chaining (linked lists) or open addressing. This allows fast average-case O(1) time for insertion, deletion, and lookup.
