What is the purpose of a .gitignore file?

Answer: A .gitignore file specifies intentionally untracked files that Git should ignore when performing operations like add, commit, and status. It helps keep the repository clean by excluding files generated by build processes, IDEs, temporary files, and other non-essential files, ensuring that only relevant code is tracked and shared.
